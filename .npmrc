# NPM Configuration
# Use exact versions for reproducible builds
save-exact=false

# Use npm ci for faster, reliable builds
# Prefer package-lock.json over package.json
package-lock=true

# Engine strict mode (warn on incompatible Node versions)
engine-strict=false

# Log level
loglevel=warn

# Fund messages
fund=false

# Audit level
audit-level=moderate

